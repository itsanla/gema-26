services:
    database:
        image : postgres:17-bookworm
        container_name : database
        ports:
            - "5432:5432"
        environment:
            - POSTGRES_USER=itsanla
            - POSTGRES_PASSWORD=070078
            - POSTGRES_DB=db-jamur
        volumes:
            - postgresql:/var/lib/postgresql/data

volumes:
    postgresql:
        